\section{Generation and Baseline Details}

\subsection{Complementary Reference Generation}

We use the same revision of Qwen2.5-7B-Instruct for the model and tokenizer:

\begin{center}\small
\texttt{fe11104b620d588ccc049ff6631dd3ea002e3d98}
\end{center}

The unquantized BF16 model runs with MLX-LM 0.31.2. Decoding uses a
temperature of 0.7, top-$p$ of 0.9, top-$k$ of 20, and a maximum of 256 new
tokens. Queries are sorted by identifier and processed in batches of eight.
Each batch seed is derived from the protocol version, dataset, prompt hash, and
batch index using the first 64 bits of their SHA-256 digest.

The system prompt is:

\begin{quote}\small
You generate evidence-like reference passages for information retrieval.

Given one search query, return exactly five concise, complementary passages
that could plausibly occur in relevant documents. Preserve every named entity,
relation, number, polarity, temporal condition, scope, and constraint in the
query. Add useful aliases, technical terminology, and paraphrases, but do not
broaden the information need or invent a specific answer. Each passage must be
one sentence of at most 25 words.

Return one single-line JSON array containing exactly five distinct strings.
Count the five array items before responding. Do not output an object key,
query identifier, Markdown, placeholders, or any other text. Every item must
contain a substantive passage.
\end{quote}

The query is supplied in a canonical JSON object with a single \texttt{query}
field. XGrammar 0.2.1 constrains the response to a JSON array containing exactly
five strings. Validation rejects empty passages, placeholders, and duplicates.
An invalid response is retried once with the same seed, the previous response,
and validation feedback. If the retry also fails, the failure is recorded and
the corresponding query and generation run fall back to the unexpanded query.

The second generator is Mistral-7B-Instruct-v0.3 at revision

\begin{center}\small
\texttt{c170c708c41dac9275d15a8fff4eca08d52bab71}.
\end{center}

It uses the same decoding, batching, and validation settings.

\subsection{Query Construction}

All averaging, projection, and normalization operations in orthogonal residual
expansion use float32. Shared expansion uses the same representations of the
query-reference pairs, but averages and normalizes them without removing the
component parallel to the original query. Each pair is encoded as
\texttt{query [SEP] reference}.

For sparse retrieval, the expanded query contains one copy of the original
query followed by the selected reference passages. BM25 scores under the
original and expanded queries are stored separately before applying the
document-wise score product. The binary-mask ablation retains the
expanded-query score for documents matched by both queries. The references-only
ablation removes the original query before retrieval. Our construction neither
repeats the original query nor uses the MuGI repetition coefficient.

\subsection{Baseline Reproduction}

All generative baselines use the same primary generator so that their
differences mainly reflect query construction rather than model capacity. The
exact prompts are included in the accompanying repository.

HyDE generates eight hypothetical texts per run, encodes them independently as
documents, and averages their normalized representations. Its sparse channel
uses the original query. FiQA, ArguAna, SciFact, and TREC-COVID use
task-specific generation instructions; the remaining datasets use a general
retrieval instruction. HyDE allows up to 512 new tokens per generation.

Query2doc generates one pseudo-relevant document of at most 120 words. Dense
retrieval encodes the concatenation of the original query and pseudo-document.
Sparse retrieval concatenates five copies of the original query with the
pseudo-document.

MuGI generates five pseudo-references. Dense retrieval averages the
representations obtained by concatenating the original query with each
reference. Sparse retrieval repeats the original query

\[
\left\lfloor\frac{|R|}{|q_o|\beta}\right\rfloor
\]

times before appending all references, where $|\cdot|$ denotes character length
and $\beta=4$. We reproduce MuGI's generation and query integration components
without its pseudo-relevance feedback calibration.

\section{Operator Properties and Mechanism Diagnostics}

\subsection{Properties of the Channel Operators}

For the Dense construction, each contextual vector $\mathbf v_i$ is unit
normalized. The triangle inequality gives

\[
\|\bar{\mathbf v}_r\|
\leq \frac{1}{n}\sum_{i=1}^{n}\|\mathbf v_i\|=1.
\]

Orthogonal projection cannot increase the norm, so
$\|\mathbf z\|\leq 1$. Since $\mathbf v_o^\top\mathbf z=0$ and
$\|\mathbf v_o\|=1$,

\[
\begin{aligned}
\mathbf e_D^\top\mathbf v_o
&=\frac{1}{\sqrt{1+\|\mathbf z\|^2}},\\
\angle(\mathbf e_D,\mathbf v_o)
&=\arctan\|\mathbf z\|\leq\frac{\pi}{4}.
\end{aligned}
\]

If $\mathbf z=\mathbf 0$, normalization leaves $\mathbf v_o$ unchanged.

For Sparse, non-negativity gives
$\operatorname{supp}(s_os_r)=
\operatorname{supp}(s_o)\cap\operatorname{supp}(s_r)$. Additivity and the
inclusion of $q_o$ in $q_r$ imply
$\operatorname{supp}(s_o)\subseteq\operatorname{supp}(s_r)$, yielding exact
support preservation. If $s_r=c\,s_o$ for $c>0$, then
$s_A=c\,s_o^2$; squaring is strictly increasing on positive scores, so the
ranking is unchanged. Multiplying $s_o$ or $s_r$ by a positive global constant
only rescales every anchored score and likewise leaves the ranking unchanged.

\subsection{Mechanism Diagnostics}

Table~\ref{tab:mechanism-diagnostics} reports the quantities used to inspect
the realized operators. Dense angle is measured between the original and
expanded query vectors. Sparse turnover is one minus Top-20 overlap with the
original sparse ranking. Relevant RR change averages the difference in
reciprocal rank for judged relevant documents that appear in the exported
rankings. All macro values give each dataset equal weight.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5.0pt}
\begin{tabular}{lrrrr}
\toprule
Dataset & Dense angle ($^\circ$) & Sparse Top-20 turnover (\%) & Relevant RR change & Support retained (\%) \\
\midrule
SciFact        &  9.38 & 18.88 & .0211 & 100.0000 \\
NFCorpus       & 17.65 & 18.69 & .0147 & 100.0000 \\
TREC-COVID     & 12.99 & 45.37 & .0021 & 100.0000 \\
FiQA           & 13.34 & 33.91 & .0204 & 100.0000 \\
ArguAna        &  4.02 &  8.14 & .0048 & 100.0000 \\
Touch\'e-2020 & 14.40 & 37.07 & .0091 &  99.9999 \\
SCIDOCS        & 12.05 & 20.98 & .0028 & 100.0000 \\
\midrule
Equal-dataset macro & 11.98 & 26.15 & .0107 & 100.0000 \\
\bottomrule
\end{tabular}
\caption{Mechanism diagnostics computed from the frozen query constructions
and rankings. Support retained is the mean per-query--draw fraction of the
original sparse support; the displayed macro rounds to 100.0000\%.}
\label{tab:mechanism-diagnostics}
\end{table*}

The within-dataset quartiles in Figure~\ref{fig:mechanism-bins} are not
monotonic. Larger Dense angles do not consistently improve either nDCG or
access depth, and greater Sparse turnover does not consistently do so either.
The descriptive correlations lead to the same distinction: relevant-document
reciprocal-rank gain has a mean Spearman correlation of .391 with Sparse-only
nDCG gain, whereas overall turnover has a correlation of .078. The corresponding
Dense-angle correlations with nDCG gain and depth reduction are .010 and .011.
These observations do not support an angle or turnover threshold.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig_mechanism_binned_effects.pdf}
\caption{Equal-dataset means after assigning queries to within-dataset
quartiles of Dense angle or Sparse Top-20 turnover. Positive depth reduction
means shallower replay. The nonmonotonic curves are reported as descriptive
mechanism diagnostics, not as a basis for threshold selection.}
\label{fig:mechanism-bins}
\end{figure*}

The ideal support-preservation property assumes a non-negative additive
Sparse scorer and complete score export. It holds exactly in the exported
rankings for six datasets. On Touch\'e-2020, 23 of 147 query--draw pairs omit
one or two extremely low-scoring tail documents, for 25 omissions in total.
Mean support retention is 99.99990\%, and the minimum is 99.99881\%. We retain
these cases in the diagnostic artifacts and do not treat the practical Lucene
export as exactly support preserving on this dataset.

\section{Retrieval and Access Analysis}
\label{sec:appendix-analysis}

\subsection{Per-Dataset Controlled Results}

Table~\ref{tab:per-dataset-controlled} exposes the dataset-level values behind
the controlled macro comparison. The three methods use identical generated
references where applicable. Sparse support is the mean number of
positive-score documents; exhaustion is the fraction of query--draw rankings
whose finite Sparse support is exhausted during replay.

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.1pt}
\begin{tabular}{llrrrrrrr}
\toprule
Dataset & Method & Queries & nDCG@10 & Recall@20 & $L_D$ & $L_S$ & Sparse support & Exhaust. (\%) \\
\midrule
ArguAna & Original & 1406 & .3941 & .9203 & 157.1 & 156.7 & 8587.8 & 0.00 \\
        & Shared   &      & .4004 & .9265 & 123.4 & 123.0 & 8616.8 & 0.00 \\
        & DESA     &      & .3991 & .9237 & 133.1 & 132.7 & 8587.8 & 0.00 \\
\addlinespace
FiQA & Original & 648 & .3649 & .5260 & 2179.0 & 2076.3 & 28384.6 & 1.23 \\
     & Shared   &     & .3635 & .5391 & 1104.1 & 1103.6 & 46673.3 & 0.00 \\
     & DESA     &     & .3780 & .5450 & 1096.2 & 1065.6 & 28384.6 & 0.93 \\
\addlinespace
NFCorpus & Original & 323 & .3670 & .2099 & 474.4 & 173.7 & 561.5 & 48.61 \\
         & Shared   &     & .3780 & .2260 & 359.1 & 357.6 & 3211.6 & 1.14 \\
         & DESA     &     & .3793 & .2195 & 403.1 & 133.5 & 561.5 & 46.75 \\
\addlinespace
SCIDOCS & Original & 1000 & .1923 & .2721 & 694.8 & 599.3 & 11503.0 & 2.70 \\
        & Shared   &      & .1905 & .2790 & 372.7 & 372.2 & 23239.6 & 0.00 \\
        & DESA     &      & .1940 & .2787 & 448.8 & 387.2 & 11503.0 & 1.63 \\
\addlinespace
SciFact & Original & 300 & .7253 & .9036 & 409.1 & 368.6 & 2765.7 & 4.33 \\
        & Shared   &     & .7325 & .9182 & 284.1 & 282.6 & 4489.0 & 0.33 \\
        & DESA     &     & .7427 & .9200 & 318.3 & 291.6 & 2765.7 & 3.89 \\
\addlinespace
TREC-COVID & Original & 50 & .7781 & .0398 & 6270.9 & 4729.9 & 92683.8 & 2.00 \\
           & Shared   &    & .8169 & .0433 & 787.3 & 786.9 & 141483.1 & 0.00 \\
           & DESA     &    & .8224 & .0435 & 1574.8 & 1574.4 & 92683.8 & 0.00 \\
\addlinespace
Touch\'e-2020 & Original & 49 & .3786 & .3382 & 2081.8 & 2081.4 & 151619.0 & 0.00 \\
              & Shared   &    & .3883 & .3344 & 1221.5 & 1221.3 & 284677.0 & 0.00 \\
              & DESA     &    & .4071 & .3560 & 1127.8 & 1127.4 & 151618.9 & 0.00 \\
\bottomrule
\end{tabular}
\caption{Per-dataset controlled results for the seven evaluation collections.}
\label{tab:per-dataset-controlled}
\end{table*}

\subsection{Primary Statistical Tests}

Table~\ref{tab:primary-tests} reports the complete seven-dataset statistical
record. For retrieval metrics, the favorable difference is DESA minus the
comparator; for depth, it is the comparator minus DESA, so positive values
always favor DESA. Holm correction is applied over the four outcomes
separately for each prespecified comparator.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{1.8pt}
\begin{tabular}{lll}
\toprule
Comparison / outcome & Effect [95\% CI] & $p/p_{\mathrm{Holm}}$ \\
\midrule
Original / nDCG@10   & .0175 [.0111, .0238]    & .0001 / .0004 \\
Original / Recall@20 & .0109 [.0070, .0151]    & .0001 / .0004 \\
Original / $L_D$     & 1023.6 [377.5, 2012.6]  & .0001 / .0004 \\
Original / $L_S$     & 781.9 [356.1, 1339.7]   & .0001 / .0004 \\
\addlinespace
Shared / nDCG@10     & .0075 [.0036, .0116]     & .0003 / .0012 \\
Shared / Recall@20   & .0028 [-.0003, .0061]    & .0912 / .2736 \\
Shared / $L_D$       & -121.4 [-367.0, 39.7]    & .3157 / .6313 \\
Shared / $L_S$       & -66.5 [-310.6, 93.0]     & .7909 / .7909 \\
\bottomrule
\end{tabular}
\caption{Stratified paired tests across the seven evaluation collections after
averaging the three generation draws within query.}
\label{tab:primary-tests}
\end{table}

\subsection{Cutoff, Operator-Control, and QuDAR Details}

Table~\ref{tab:fixed-l} reports the complete macro nDCG@10 values behind the
fixed-cutoff analysis.

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{2.7pt}
\begin{tabular}{lrrrrrrrr}
\toprule
Method & $L=10$ & $L=20$ & $L=50$ & $L=100$ & $L=200$ & $L=500$ & $L=1000$ & Complete \\
\midrule
No expansion        & .4478 & .4489 & .4512 & .4553 & .4565 & .4569 & .4571 & \textbf{.4572} \\
Shared              & .4630 & .4633 & .4641 & .4645 & .4666 & .4670 & \textbf{.4671} & \textbf{.4671} \\
Dense only          & .4566 & .4568 & .4582 & .4608 & .4617 & \textbf{.4622} & .4621 & .4621 \\
Sparse only         & .4661 & .4676 & .4654 & .4686 & .4704 & .4713 & \textbf{.4715} & .4714 \\
MuGI-style (controlled) & .4612 & .4615 & .4608 & .4613 & .4630 & .4636 & .4637 & \textbf{.4638} \\
Sparse binary mask  & .4635 & .4637 & .4639 & .4649 & .4670 & .4672 & \textbf{.4675} & \textbf{.4675} \\
Reference-only sparse & .4534 & .4537 & .4529 & .4537 & .4555 & .4559 & .4564 & \textbf{.4565} \\
DESA                & .4738 & .4727 & .4709 & .4727 & .4740 & .4743 & \textbf{.4747} & \textbf{.4747} \\
\bottomrule
\end{tabular}
\caption{Equal-dataset macro nDCG@10 under fixed top-$L$ and complete-list
fusion on the seven evaluation datasets. Bold marks the maximum within each
row.}
\label{tab:fixed-l}
\end{table*}

Table~\ref{tab:cutoff-query-diagnostics} reports the query--draw diagnostic
behind the fixed-cutoff discussion. Conclusion change compares the sign of the
method--Original difference under fixed-$L$ and complete-list fusion. Strict
reversal excludes changes to or from a tie.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.0pt}
\begin{tabular}{rrrrrr}
\toprule
$L$ & Exact Top-20 & \multicolumn{2}{c}{nDCG (\%)} & \multicolumn{2}{c}{Recall (\%)} \\
 & (\%) & Change & Reverse & Change & Reverse \\
\midrule
20   & 1.92 & 18.23 & 8.84 & 22.50 & 3.84 \\
50   & 16.54 & 6.95 & 3.07 & 18.84 & 3.06 \\
100  & 47.40 & 2.17 & 0.73 & 11.74 & 0.99 \\
1000 & 92.62 & 0.19 & 0.01 & 1.18 & 0.00 \\
\bottomrule
\end{tabular}
\caption{DESA query--draw sensitivity to fixed Top-$L$, macro-averaged with
equal dataset weight. Exact Top-20 compares with complete-list fusion;
conclusion columns compare DESA with Original.}
\label{tab:cutoff-query-diagnostics}
\end{table}

Table~\ref{tab:operator-controls} isolates the transformation applied within
each channel. The control pairs use identical generated references and keep
the other channel at its original-query ranking. Effects average the three
draws within query before paired inference.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{2.8pt}
\begin{tabular}{@{}lrr@{}}
\toprule
Construction & nDCG@10 & Recall@20 \\
\midrule
Original          & .4572 & .4586 \\
Contextual Dense  & .4622 & .4637 \\
Residual Dense    & .4621 & .4637 \\
Rewrite Sparse    & .4686 & .4670 \\
Product Sparse    & .4714 & .4678 \\
\midrule
\multicolumn{3}{l}{Paired effect (proposed $-$ control)} \\
Dense residual, nDCG   & \multicolumn{2}{l}{$-.00005$ [$-.00086,.00082$], $p_H=1.0$} \\
Dense residual, Recall & \multicolumn{2}{l}{$-.00006$ [$-.00043,.00033$], $p_H=1.0$} \\
Sparse product, nDCG   & \multicolumn{2}{l}{$ .00283$ [$-.00126,.00693$], $p_H=.3650$} \\
Sparse product, Recall & \multicolumn{2}{l}{$ .00078$ [$-.00184,.00331$], $p_H=.5689$} \\
\bottomrule
\end{tabular}
\caption{Equal-dataset macro functional controls and query-level paired
effects. Holm correction is over nDCG and Recall within each control pair.}
\label{tab:operator-controls}
\end{table}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{2.9pt}
\begin{tabular}{lrrrr}
\toprule
Dataset & \multicolumn{2}{c}{nDCG@10} & \multicolumn{2}{c}{Recall@20} \\
 & DESA & QuDAR & DESA & QuDAR \\
\midrule
ArguAna        & .3991 & .3985 & .9237 & .9253 \\
FiQA           & .3780 & .3833 & .5450 & .5554 \\
NFCorpus       & .3793 & .3780 & .2195 & .2227 \\
SCIDOCS        & .1940 & .1957 & .2787 & .2811 \\
SciFact        & .7427 & .7403 & .9200 & .9211 \\
TREC-COVID     & .8224 & .8233 & .0435 & .0438 \\
Touch\'e-2020 & .4071 & .4137 & .3560 & .3500 \\
\midrule
Equal-dataset macro & .4747 & .4761 & .4695 & .4714 \\
\bottomrule
\end{tabular}
\caption{Matched-evidence comparison with QuDAR-simple RRF. QuDAR fuses the
Top-1000 OS, ES, OD, and ED rankings with RRF constant 60. DESA--QuDAR paired
effects are $-.00145$ [$-.00478,.00181$] for nDCG and $-.00188$
[$-.00400,.00019$] for Recall, with $p_H=.3966$ and $.1668$.}
\label{tab:qudar-matched}
\end{table}

\subsection{Additional Ablations and Robustness}
\label{sec:additional-results}

The binary-mask ablation retains the same Sparse support as score-product
anchoring but discards the magnitude of the original-query score. Replacing the
score product with this mask lowers macro nDCG@10 by 1.54\% and Recall@20 by
0.74\%. The graded original score therefore improves the ordering of documents
within the retained support.

Reference count affects both effectiveness and access depth. Increasing the
number of complementary passages from one to three improves nDCG@10 by 1.91\%
and reduces Dense and Sparse stopping depths by 25.46\% and 26.61\%. Increasing
the count from three to five adds 0.35\% nDCG@10 and 0.43\% Recall@20, with
further depth reductions of 6.05\% and 5.62\%.

With Mistral as the generator, DESA produces non-negative nDCG@10 and Recall@20
changes on every dataset in the sampled four-dataset subset. Relative to
Original, Dense depth reductions range from 7.12\% to 47.61\%, and Sparse depth
reductions range from 7.16\% to 47.62\%. On ArguAna, 10\% of the 100 sampled
queries fall back to Original after invalid structured output.

Replacing BGE with Contriever also improves both retrieval metrics on all four
sampled datasets. Touch\'e-2020 is the observed access-depth failure case:
Dense and Sparse stopping depths increase by 14.29\% and 4.34\%, respectively.

Across the 25k, 50k, 100k, and 171k TREC-COVID corpora, nDCG@10 gains range
from 3.69\% to 5.78\%, and Recall@20 gains range from 5.56\% to 9.21\%. The
Dense and Sparse depth reductions are 68.12\%/68.16\%, 65.11\%/65.14\%,
79.83\%/79.84\%, and 74.89\%/66.71\%, respectively. The access benefit remains
substantial at every tested size and reaches its maximum at 100k documents.

\subsection{Retrieval Implementation}

BGE-small-en-v1.5 uses revision

\begin{center}\small
\texttt{5c38ec7c405ec4b44b94cc5a9bb96e735b38267a}.
\end{center}

Queries are prefixed with
``Represent this sentence for searching relevant passages:'' and encoded
with a maximum length of 512 tokens. Contriever uses mean pooling and revision

\begin{center}\small
\texttt{2bd46a25019aeea091fd42d1f0fd4801675cf699}.
\end{center}

All query and document embeddings are normalized.

Pyserini 2.3.0 runs with Java 21 and uses \texttt{DefaultEnglishAnalyzer}. A
separate Lucene index is built for each dataset and each TREC-COVID snapshot,
so BM25 collection statistics are computed within the corresponding corpus.
Documents with equal retrieval or fusion scores are ordered by the UTF-8 order
of their identifiers.

\subsection{Access-Depth Replay}

The replay begins with empty Dense and Sparse prefixes. After $l_c$ items have
been read from channel $c$, the maximum contribution of its next unread
document is

\[
u_c(l_c)=\frac{1}{60+l_c+1}.
\]

The bound is zero after a channel is exhausted. At each step, the replay reads
from the channel with the larger next-unread bound, breaking ties in favor of
Dense.

For an observed document, its accumulated contribution is a score lower bound.
If the document has not appeared in the other channel, that channel's
next-unread contribution supplies an additional upper bound. A document unseen
in both channels is bounded by the sum of the two next-unread contributions.

The replay stops when these bounds certify both the membership and order of the
current top-20. The stopping depths are recorded as
$(L_D,L_S)=(l_D,l_S)$.

\subsection{Artifact Traceability}

Each generation record contains the raw response, parsed passages, prompt and
model hashes, decoding parameters, seed, retry history, runtime lock, and code
commit. Dense and Sparse rankings are stored by channel in a compressed ranking
store and assigned SHA-256 hashes. Each query-level result contains the method,
generation run, effectiveness metrics, $(L_D,L_S)$, sparse support and
exhaustion status, ordered top-20, and hashes of the generation, rankings,
method configuration, and replay trace.

The Python environment is fixed by \texttt{uv.lock}, and the Java version is
checked before retrieval. Before final evaluation, the code,
configurations, prompts, source inputs, model artifacts, generations, and
ranking stores are recorded in a hash manifest. All reported tables are
reconstructed from the resulting query-level records.
